import React from 'react';
import myAlart from '~/myAlart';

function Button() {
    return (
        <button onClick={myAlart}>
            Click me
        </button>
    );
}

export default Button;